import { Meta } from '@storybook/addon-docs';
import { colors } from '../styles/theme';
import { Shape } from '../components';

<Meta title="Documentation/Welcome" />

<style>{`
    .bg {
        position: absolute;
        z-index: 0;
    }

  .subheading {
      z-index: 30;
    --mediumdark: ${colors.coral};
    font-weight: 900;
    font-size: 13px;
    color: ${colors.violet};
    letter-spacing: 6px;
    line-height: 24px;
    text-transform: uppercase;
    margin-bottom: 12px;
    margin-top: 40px;
    text-align: right;
  }

  .link-list {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr 1fr;
    row-gap: 10px;
    z-index: 10;
  }

  @media (min-width: 620px) {
    .link-list {
      row-gap: 20px;
      column-gap: 20px;
      grid-template-columns: 1fr 1fr;
    }
  }

  @media all and (-ms-high-contrast:none) {
  .link-list {
      display: -ms-grid;
      -ms-grid-columns: 1fr 1fr;
      -ms-grid-rows: 1fr 1fr;
    }
  }

  .link-item {
    display: block;
    padding: 20px 30px 20px 15px;
    border: 1px solid #00000010;
    background-color: rgba(255,255,255, 0.5);
    border-radius: 5px;
    transition: background 150ms ease-out, border 150ms ease-out, transform 150ms ease-out;
    color: ${colors.purple};
    display: flex;
    align-items: flex-start;
    z-index: 20;
  }

  .link-item:hover {
    border-color: ${colors.coral};
    transform: translate3d(0, -3px, 0);
    box-shadow: rgba(0, 0, 0, 0.08) 0 3px 10px 0;
  }

  .link-item:active {
    border-color: ${colors.lime};
    transform: translate3d(0, 0, 0);
  }

  .link-item strong {
    font-weight: 700;
    display: inline;
    margin-bottom: 2px;
  }

  .link-item span {
    font-size: 14px;
    line-height: 20px;
  }

  .tip {
    display: inline-block;
    border-radius: 1em;
    font-size: 11px;
    line-height: 12px;
    font-weight: 700;
    background: ${colors.lime};
    color: ${colors.purple};
    padding: 4px 12px;
    margin-right: 10px;
    vertical-align: top;
  }

  .tip-wrapper {
    font-size: 13px;
    line-height: 20px;
    margin-top: 40px;
    margin-bottom: 40px;
  }

  .tip-wrapper code {
    font-size: 12px;
    display: inline-block;
  }

`}</style>

# futureproof storybook

<div className="bg left">
  <Shape kind="angles" color="violet" width="800" />
</div>

<div className="subheading">Welcome!</div>

<div className="link-list">
  <a
    className="link-item"
    href="https://www.npmjs.com/package/@getfutureproof/fpsb"
    target="_blank"
  >
    <span>
      <strong>fpsb on npm</strong> is our library of reusable React components
      for internal futureproof projects.
    </span>
  </a>
  <div className="tip">
    <span className="tip-wrapper">
      Check out the <strong>existing stories</strong> in the sidebar. Each set
      of stories has a <code>Canvas</code> and a <code>Docs</code> tab (look
      above!). Have a look around, grab a cup of tea and settle down for some
      stories.
    </span>
  </div>
  <a
    className="link-item"
    href="https://github.com/getfutureproof-projects/fpsb"
    target="_blank"
  >
    <span>
      When the feeling takes you, we'd love to <strong>add your stories</strong>!
    </span>
  </a>
  <a
    className="link-item"
    href="https://github.com/getfutureproof-projects/fpsb/issues/new"
    target="_blank"
  >
    <span>
      Got an <strong>Issue?</strong> Let us know on GitHub!
    </span>
  </a>
</div>
